sentence-transformers>=3.0,<4.0
python-telegram-bot>=22.5,<23
mlx-audio>=0.5.4,<0.6
kokoro-onnx>=0.4.7,<1.0
soundfile>=0.12,<1.0
aiohttp>=3.9,<4.0
pywebpush>=2.0,<3.0
# Dory graph memory. The native Ollama extraction backend needs the
# local-model JSON fix (dory/pipeline/llm_util.py), which is not on PyPI
# yet — 1.0.2 is the latest release and predates it. Until it ships,
# install from the local checkout:  pip install -e ../Dory[ollama]
dory-memory[ollama]>=1.0.2
mcp>=1.0,<2.0
pypdf>=4.0,<6.0
icalendar>=6,<8
recurring-ical-events>=3,<4
